version: '3.9'

services:
  db:
    image: postgres:15-alpine
    container_name: soroscan-db
    environment:
      POSTGRES_DB: soroscan
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "${POSTGRES_PORT:-5432}:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 5s
      timeout: 5s
      retries: 5
    networks:
      - soroscan-network

  redis:
    image: redis:7-alpine
    container_name: soroscan-redis
    ports:
      - "${REDIS_PORT:-6379}:6379"
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 3s
      retries: 5
    networks:
      - soroscan-network

  web:
    build:
      context: ./django-backend
      dockerfile: Dockerfile
    container_name: soroscan-web
    command: >
      sh -c "python manage.py migrate --noinput &&
             python manage.py runserver 0.0.0.0:8000"
    volumes:
      - ./django-backend:/app
    ports:
      - "${WEB_PORT:-8000}:8000"
    env_file:
      - ./django-backend/.env
    environment:
      - DATABASE_URL=postgresql://postgres:postgres@db:5432/soroscan
      - REDIS_URL=redis://redis:6379/0
      - DEBUG=True
      - ALLOWED_HOSTS=*
      - FRONTEND_BASE_URL=http://localhost:3000
      - STELLAR_TESTNET_RPC_URL=${STELLAR_TESTNET_RPC_URL:-https://soroban-testnet.stellar.org}
      - STELLAR_TESTNET_HORIZON_URL=${STELLAR_TESTNET_HORIZON_URL:-https://horizon-testnet.stellar.org}
      - STELLAR_TESTNET_NETWORK_PASSPHRASE=${STELLAR_TESTNET_NETWORK_PASSPHRASE:-Test SDF Network ; September 2015}
      - STELLAR_MAINNET_RPC_URL=${STELLAR_MAINNET_RPC_URL:-https://soroban-mainnet.stellar.org}
      - STELLAR_MAINNET_HORIZON_URL=${STELLAR_MAINNET_HORIZON_URL:-https://horizon.stellar.org}
      - STELLAR_MAINNET_NETWORK_PASSPHRASE=${STELLAR_MAINNET_NETWORK_PASSPHRASE:-Public Global Stellar Network ; September 2015}
    depends_on:
      db:
        condition: service_healthy
      redis:
        condition: service_healthy
    networks:
      - soroscan-network
    stdin_open: true
    tty: true

  worker:
    build:
      context: ./django-backend
      dockerfile: Dockerfile
    container_name: soroscan-worker
    command: celery -A soroscan worker --loglevel=info --concurrency=2
    volumes:
      - ./django-backend:/app
    env_file:
      - ./django-backend/.env
    environment:
      - DATABASE_URL=postgresql://postgres:postgres@db:5432/soroscan
      - REDIS_URL=redis://redis:6379/0
      - DEBUG=True
      - STELLAR_TESTNET_RPC_URL=${STELLAR_TESTNET_RPC_URL:-https://soroban-testnet.stellar.org}
      - STELLAR_TESTNET_HORIZON_URL=${STELLAR_TESTNET_HORIZON_URL:-https://horizon-testnet.stellar.org}
      - STELLAR_TESTNET_NETWORK_PASSPHRASE=${STELLAR_TESTNET_NETWORK_PASSPHRASE:-Test SDF Network ; September 2015}
      - STELLAR_MAINNET_RPC_URL=${STELLAR_MAINNET_RPC_URL:-https://soroban-mainnet.stellar.org}
      - STELLAR_MAINNET_HORIZON_URL=${STELLAR_MAINNET_HORIZON_URL:-https://horizon.stellar.org}
      - STELLAR_MAINNET_NETWORK_PASSPHRASE=${STELLAR_MAINNET_NETWORK_PASSPHRASE:-Public Global Stellar Network ; September 2015}
    depends_on:
      db:
        condition: service_healthy
      redis:
        condition: service_healthy
    networks:
      - soroscan-network

  beat:
    build:
      context: ./django-backend
      dockerfile: Dockerfile
    container_name: soroscan-beat
    command: celery -A soroscan beat --loglevel=info
    volumes:
      - ./django-backend:/app
    env_file:
      - ./django-backend/.env
    environment:
      - DATABASE_URL=postgresql://postgres:postgres@db:5432/soroscan
      - REDIS_URL=redis://redis:6379/0
      - DEBUG=True
      - STELLAR_TESTNET_RPC_URL=${STELLAR_TESTNET_RPC_URL:-https://soroban-testnet.stellar.org}
      - STELLAR_TESTNET_HORIZON_URL=${STELLAR_TESTNET_HORIZON_URL:-https://horizon-testnet.stellar.org}
      - STELLAR_TESTNET_NETWORK_PASSPHRASE=${STELLAR_TESTNET_NETWORK_PASSPHRASE:-Test SDF Network ; September 2015}
      - STELLAR_MAINNET_RPC_URL=${STELLAR_MAINNET_RPC_URL:-https://soroban-mainnet.stellar.org}
      - STELLAR_MAINNET_HORIZON_URL=${STELLAR_MAINNET_HORIZON_URL:-https://horizon.stellar.org}
      - STELLAR_MAINNET_NETWORK_PASSPHRASE=${STELLAR_MAINNET_NETWORK_PASSPHRASE:-Public Global Stellar Network ; September 2015}
    depends_on:
      db:
        condition: service_healthy
      redis:
        condition: service_healthy
    networks:
      - soroscan-network

  frontend:
    build:
      context: .
      dockerfile: soroscan-frontend/Dockerfile.frontend
    container_name: soroscan-frontend
    ports:
      - "3000:3000"
    environment:
      - BACKEND_BASE_URL=http://web:8000
    depends_on:
      - web
    networks:
      - soroscan-network

volumes:
  postgres_data:

networks:
  soroscan-network:
    driver: bridge
