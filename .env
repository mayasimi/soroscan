notepad .env
```

When Notepad opens, look for this line and put a `#` at the start of it:
```
DATABASE_URL=postgresql://postgres:postgres@db:5432/soroscan
```

So it becomes:
```
#DATABASE_URL=postgresql://postgres:postgres@db:5432/soroscan